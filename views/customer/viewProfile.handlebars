<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Profile</title>
    <link rel="stylesheet" href="/css/global.css"> <!-- Global styles (if any) -->
    <link rel="stylesheet" href="/css/profile.css"> <!-- Profile specific styles -->
</head>

<body>
    <div class="profile-container">
        <h1>Customer Profile</h1>
        {{#if customer}}
        <div class="profile-details">
            <h2>Profile Details</h2>
            <p><strong>ID:</strong> {{customer.id}}</p>
            <p><strong>Email:</strong> {{customer.email}}</p>
            <p><strong>Role:</strong> {{customer.role}}</p>
            <!-- You can add more customer attributes here if needed -->
        </div>
        {{else}}
        <p class="not-found">Customer not found.</p>
        {{/if}}
    </div>
</body>

</html>