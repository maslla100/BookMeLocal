<div class="admin-dashboard">
    <h1>Admin Dashboard</h1>

    <div class="statistics">
        <p>Total Users: {{userCount}}</p>
        <p>Total Bookings: {{bookingCount}}</p>
        <p>Total Businesses: {{businessCount}}</p>
        <p>Total Services: {{serviceCount}}</p>
    </div>

    <div class="admin-actions">
        <a href="{{links.manageUsers}}">Manage Users</a>
        <a href="{{links.manageBusiness}}">Manage Businesses</a>
        <a href="{{links.manageServices}}">Manage Services</a>
        <a href="{{links.manageBookings}}">Manage Bookings</a>
        <a href="{{links.createBusiness}}">Create Business</a>
    </div>
</div>